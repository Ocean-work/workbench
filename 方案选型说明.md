# Python 文档自检逻辑接入方案说明

## 一、技术方案选型

### 方案对比

| 对比项 | 方案A：Pyodide | 方案B：翻译为 TypeScript | 方案C：其他 WASM |
|--------|---------------|------------------------|-----------------|
| 代码复用 | ✅ 直接复用 Python 脚本 | ❌ 全部重写 | ⚠️ 需额外适配 |
| docx 解析 | ✅ python-docx 成熟稳定 | ⚠️ JS 库功能有限 | ❌ 无现成方案 |
| Word 批注 | ✅ lxml 直接操作 XML | ❌ JS 几乎不支持 | ❌ 无 |
| 开发工作量 | 中（适配浏览器环境） | 大（重写所有逻辑） | 大 |
| 首次加载 | ~20MB（Pyodide 运行时） | 快 | 不确定 |
| 后续扩展 | ✅ 直接加 Python 规则 | ⚠️ 每次都要翻译 | ⚠️ 复杂 |
| GitHub Pages | ✅ 纯静态 | ✅ 纯静态 | ✅ 纯静态 |

### 最终选择：方案A - Pyodide

**核心理由：**
1. **代码复用最大化**：现有 Python 检查逻辑（doc_selfcheck.py、cross_check_v3.py）可直接移植，避免重写
2. **docx 处理能力强**：python-docx + lxml 组合可完整支持格式检查和 Word 批注生成
3. **纯静态部署**：Pyodide 通过 CDN 加载，不依赖后端服务，完美适配 GitHub Pages
4. **后续扩展容易**：新增检查规则只需修改 Python 脚本，无需翻译为 JS

## 二、架构设计

```
┌─────────────────────────────────────────────────┐
│                   浏览器端                       │
│  ┌─────────────┐     ┌──────────────────────┐  │
│  │ DocSelfCheck │────▶│  pyodideService.ts   │  │
│  │   (React)    │     │  (前端封装层)        │  │
│  └─────────────┘     └──────────┬───────────┘  │
│                                 │              │
│                         ┌───────▼───────┐      │
│                         │    Pyodide     │      │
│                         │ (WebAssembly)  │      │
│                         └───────┬───────┘      │
│                                 │              │
│                         ┌───────▼───────┐      │
│                         │ selfcheck_    │      │
│                         │ engine.py     │      │
│                         │ (Python 引擎) │      │
│                         └───────────────┘      │
│          ▲                                     │
│          │ 加载规则 JSON                        │
│  ┌───────┴────────┐                            │
│  │  rules/ 目录   │  (9套模板规则库)           │
│  └────────────────┘                            │
└─────────────────────────────────────────────────┘
```

## 三、核心文件说明

### 新增文件

| 文件路径 | 说明 |
|---------|------|
| `src/services/pyodideService.ts` | Pyodide 服务封装，负责初始化、文件转码、调用 Python 引擎 |
| `public/pyodide/selfcheck_engine.py` | 浏览器端 Python 自检引擎（适配 Pyodide 环境） |
| `public/rules/规则库_*/` | 9 套模板规则库（JSON 格式） |

### 修改文件

| 文件路径 | 说明 |
|---------|------|
| `src/pages/DocSelfCheck.tsx` | 接入真实检查逻辑，替换模拟数据 |
| `package.json` | 新增 pyodide 依赖 |

## 四、功能实现

### ✅ 已实现功能

1. **格式检查**（完整实现）
   - 标题格式：字体、字号、加粗检查
   - 正文格式：字体、字号、首行缩进检查
   - 表格格式：单元格字体、字号检查
   - 支持 9 套模板规则库（按文件名自动匹配）

2. **内容检查**（完整实现）
   - 占位符检测（未填写的占位文字）
   - 基础信息一致性检查
   - 空表格单元格检测

3. **逻辑检查**（部分实现）
   - 标题编号连续性检查
   - 文档内容完整性检查（字数统计）

4. **批注生成**（完整实现）
   - 使用 lxml 操作 Word XML
   - 在问题段落添加 Word 批注
   - 批注包含问题分类和描述

5. **下载打包**（完整实现）
   - 检查报告（Markdown）
   - 问题清单（纯文本）
   - 按文件批注（TXT）
   - 批注版 docx 文件
   - 校验说明

6. **UI 交互**（保持不变）
   - 6 步动画进度
   - 4 个 Tab 结果展示
   - zip 上传支持
   - 课程名称搜索匹配

## 五、性能说明

| 阶段 | 耗时 | 说明 |
|------|------|------|
| 首次初始化 | ~10-30秒 | 下载 Pyodide 运行时（~20MB）+ 安装 python-docx + lxml |
| 二次使用 | <1秒 | Pyodide 已缓存，直接运行 |
| 单文件检查 | <1秒 | Python 引擎处理速度快 |
| 多文件检查 | 约 N×0.5秒 | 与文件数量线性相关 |

> 💡 优化建议：可在页面空闲时预加载 Pyodide，提升用户首次体验

## 六、后续扩展建议

### 短期（1-2周）
1. **完善跨文件一致性检查**
   - 接入 cross_check_v3.py 的完整逻辑
   - 提取各文件关键字段（学时、学分、课程名称等）
   - 跨文件数据对比

2. **课程总表校验**
   - 接入课程总表 JSON 数据
   - 验证课程是否在培养方案中
   - 学时、学分等指标核对

3. **Pyodide 预加载优化**
   - 页面加载时后台预初始化
   - 显示加载进度条
   - 使用 Service Worker 缓存

### 中期（1个月）
4. **规则库在线编辑**
   - 前端提供规则库配置界面
   - 支持上传模板自动生成规则
   - 规则库版本管理

5. **PDF 报告生成**
   - 使用 Pyodide 的 reportlab 或 fpdf 库
   - 生成格式化的 PDF 检查报告
   - 支持自定义报告模板

6. **检查结果历史记录**
   - LocalStorage 保存检查历史
   - 支持查看历史检查结果
   - 结果对比功能

### 长期（3个月+）
7. **AI 辅助检查**
   - 接入 LLM API 进行语义级检查
   - 自动识别表述不清、逻辑矛盾等问题
   - 智能修改建议

8. **批量处理优化**
   - Web Worker 后台运行，不阻塞 UI
   - 支持大文件分片处理
   - 进度条细化到每个文件

9. **多语言支持**
   - 英文界面
   - 英文文档检查规则

## 七、注意事项

1. **Pyodide 版本**：当前使用 v0.26.2，如需升级请测试兼容性
2. **CDN 依赖**：Pyodide 从 jsDelivr CDN 加载，国内访问可能较慢，可考虑自建镜像
3. **内存限制**：浏览器端内存有限，超大文档（>50MB）可能影响性能
4. **规则库更新**：规则库文件在 public/rules 目录，更新后需重新构建部署
5. **浏览器兼容性**：需要支持 WebAssembly 的现代浏览器（Chrome/Edge/Firefox/Safari）

## 八、部署验证清单

- [x] 主分支代码已推送
- [x] gh-pages 分支已部署
- [x] 线上页面可访问 (HTTP 200)
- [x] Pyodide 自检脚本可访问
- [x] 规则库 JSON 可访问
- [x] 构建无错误
- [x] UI 交互保持一致
- [x] 格式检查功能完整
- [x] 批注版 docx 生成
- [x] zip 下载包包含所有文件
